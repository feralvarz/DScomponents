import { Label } from './Label';

export default {
  title: 'Design System/Input/Label',
  component: Label,
};

export const Playground = { args: { text: 'Username', error: false, required: false } };
