import { Typography } from './typography';

export default {
  title: 'Deprecated/Typography',
  component: Typography,
};

export const Playground = { args: { children: 'Hello World', variant: 'h1' } };
